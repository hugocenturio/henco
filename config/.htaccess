# Deny all direct HTTP access to files in this directory.
# config.php contains database and API credentials and must never be served to browsers.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
